qiskit>=2.0.0
# Capped below 0.48: that release relocated executor_sampler.prepare and changed its
# signature (dropped `backend`) and twirling-option validation, which the entrypoint here
# relies on. Bump the cap once the entrypoint is updated for the newer API.
qiskit-ibm-runtime>=0.47.0,<0.48
qiskit-serverless>=0.20.0
numpy>=1.26.0
pydantic>=2.0.0

# Test-only: the unit tests simulate the submitted quantum program with a seeded Aer
# sampler (FakeExecutor), because the runtime Executor has no local mode.
qiskit-aer>=0.15
